<section class="panel">
  <strong>Template Alpha</strong>
  <p style="margin: 6px 0 0; color: var(--muted); font-size: 0.9rem;">
    Workspace baseline template with inheritance.
  </p>
</section>

<section class="panel">
  <p style="margin: 0; font-size: 0.85rem; color: var(--muted);">
    {% if workspaceKey %}
      Workspace: <strong>{{ workspaceKey }}</strong>
    {% else %}
      Workspace key is not provided.
    {% endif %}
  </p>
</section>
