{
  "name": "callora-plugin-communication-admin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Admin UI bundle for the Callora Communication plugin. Builds real Vue SFCs against the host's shared Vue (external), emitting an IIFE the host admin shell loads at runtime.",
  "scripts": {
    "build": "vite build"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "vite": "^8.2.0",
    "vue": "^3.5.13"
  }
}
