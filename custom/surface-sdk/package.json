{
  "name": "@callora/surface-sdk",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Typed contract + blessed Vite preset for building Callora surface plugins (Vue islands/apps that dock into the surface runtime).",
  "exports": {
    ".": "./src/index.ts",
    "./vite-preset": "./src/vite-preset.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "peerDependencies": {
    "vue": "^3.5.0"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^5.1.0"
  },
  "devDependencies": {
    "happy-dom": "^15.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0",
    "vue": "^3.5.0"
  }
}
