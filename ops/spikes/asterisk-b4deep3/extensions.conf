; Dialplan for the B4-deep-3 spike: extension 600 answers and echoes audio back,
; so an outbound call from the SDK can prove the RTP↔media bridge round-trips.

[default]
exten => 600,1,Answer()
 same => n,Echo()
 same => n,Hangup()
