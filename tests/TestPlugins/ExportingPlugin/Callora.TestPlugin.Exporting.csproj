<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
    <IsPackable>false</IsPackable>
    <!-- Test-fixture plugin: a real, ALC-loadable IHostManagedPlugin that the
         RuntimePluginHost activation harness installs and activates. Built with
         the solution but referenced by nothing, so it only ever loads through a
         plugin load context. -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\src\Host\PluginContracts\Callora.Host.PluginContracts.csproj" />
  </ItemGroup>

</Project>
