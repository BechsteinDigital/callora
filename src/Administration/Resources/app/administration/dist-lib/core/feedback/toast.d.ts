/**
 * Severity of a toast. Mirrors the tones of CalAlert so the same outcome reads
 * the same whether it is reported transiently or in place.
 */
export type ToastTone = 'success' | 'info' | 'warning' | 'danger';
//# sourceMappingURL=toast.d.ts.map