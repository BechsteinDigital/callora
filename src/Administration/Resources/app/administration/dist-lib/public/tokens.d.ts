/**
 * The `--cal-*` design tokens as names.
 *
 * A plugin bundle does not compile against our SCSS — it can only reach runtime custom
 * properties — so the names are the contract. Styling through them is what makes a plugin page
 * follow a theme change it never knew about.
 */
export { CAL_TOKENS, readToken, type CalTokenName } from '@/core/design/tokens';
//# sourceMappingURL=tokens.d.ts.map