/**
 * Ready-made arrangements, so a plugin page looks like the shell without rebuilding its frame —
 * and gets the shell's extension points along the way rather than having to remember them.
 */
export { default as CalListPage } from '@/core/patterns/CalListPage.vue';
export { default as ExtensionSlot } from '@/core/extensions/ExtensionSlot.vue';
//# sourceMappingURL=patterns.d.ts.map