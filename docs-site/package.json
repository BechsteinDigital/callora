{
  "name": "@callora/docs-site",
  "private": true,
  "type": "module",
  "description": "The Callora documentation site (VitePress). The .NET API reference is generated separately by DocFX and linked under /api.",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "lint:md": "markdownlint-cli2",
    "lint:spell": "cspell --no-progress --no-must-find-files \"**/*.md\"",
    "lint": "npm run lint:md && npm run lint:spell"
  },
  "devDependencies": {
    "cspell": "^10.1.1",
    "markdownlint-cli2": "^0.23.2",
    "vitepress": "^1.6.3"
  }
}
